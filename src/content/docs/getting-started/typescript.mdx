---
title: TypeScript Support
description: TypeScript support in Latte
slug: getting-started/typescript
---

import { Tabs, TabItem, Badge } from '@astrojs/starlight/components';

Latte can be used with TypeScript and for TypeScript projects.

## ![TypeScript](../../../assets/TypeScript.svg) TypeScript Support

To use Latte with `TypeScript` you need to install `tsx` (https://tsx.is):

<Tabs>
    <TabItem label="npm" icon="seti:npm">
        ```bash
        npm install -D tsx
        ```
    </TabItem>
    <TabItem label="pnpm" icon="pnpm">
        ```bash
        pnpm -D install tsx
        ```
    </TabItem>
    <TabItem label="yarn" icon="seti:yarn">
        ```bash
        yarn add -D tsx
        ```
    </TabItem>
    <TabItem label="bun" icon="bun">
        ```bash
        bun add -D tsx
        ```
    </TabItem>
</Tabs>

### Change test command

```json title="package.json"
{
  "scripts": {
    "test": "NODE_OPTIONS=\"--import tsx\" latte --ts"
  }
}
```

### ![Windows](../../../assets/Windows.svg) Windows

If you're using Windows, we recommended to use `cross-env` package to set environment variables:

<Tabs>
    <TabItem label="npm" icon="seti:npm">
        ```bash
        npm install -D cross-env
        ```
    </TabItem>
    <TabItem label="pnpm" icon="pnpm">
        ```bash
        pnpm -D install cross-env
        ```
    </TabItem>
    <TabItem label="yarn" icon="seti:yarn">
        ```bash
        yarn add -D cross-env
        ```
    </TabItem>
    <TabItem label="bun" icon="bun">
        ```bash
        bun add -D cross-env
        ```
    </TabItem>
</Tabs>

And then change your test command to:

```json title="package.json"
{
  "scripts": {
    "test": "cross-env NODE_OPTIONS=\"--import tsx\" latte"
  }
}
```